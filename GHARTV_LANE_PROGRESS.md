<!-- GHARTV-MANAGED-LANE-NOTE:v1 -->

# GharTV RC8 — smooth performance review

One replacement review of the existing APK, web and owner. No production promotion by starting the launcher. This source does not deliver a FlixMomo stream relay, federated film playback, AI super-resolution or cousin-server deployment. The existing private provider inspection remains separate and on demand.

Preview defaults off; explicit12s preview or focused-channel auto mode;4.5s startup budget. Grid-focus exit cancels preview. One guide load at startup; network waits moved outside repository refresh lock. Outstanding channel HTTP calls canceled including response-body work. Focus animations removed. Full player first-frame watchdog18s after media preparation; provider authorization has its own bounded requests. Low-memory auto/explicit Light prefers720p. Local technical timing ring40 reports guide-ready, first-picture and dropped-frame observations. Normal nonbirthday starting window uses branded drawable without extra delay.

TV and browser configurable still-watching: enabled by default60min, range1–240/off,30s warning, return to guide or released-player resume screen. Only input resets it. Browser renders96 cards initially and expands as requested, with full scoped language/category/search retained.

Owner Mac & TV performance button is explicit/local-only:3 short process snapshots, memory pressure/swap/thermal/free disk, exact AVD configuration and bounded Android gfx/memory/timing reads. No process stopping, system settings or DNS changes. Before/after captures describe host state, not causal speedup proof. Missing samples are not zero. Private process information never belongs in public receipts.

Reuse same emulator5580, port8790, original signing identity and saved data. No code build/push is required on the Mac. Download/checksum/start full bundle. Prior runtime retained for rollback; known obsolete ZIPs only removed by hash. No broad Downloads, Docker or Git cleanup. Existing Obsidian and safe GitHub mirror remain; last owner-installed baseline code23/run12.

Acceptance: verify first picture and audio in actual provider account, fast focus and category switching, no preview while Find owns focus, still-watching resume/guide, web/owner manual refresh. Cloud/browser tests are controlled fixtures; no measured Mac speedup or universal entitlement is claimed.
