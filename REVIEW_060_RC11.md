# GharTV Review38 — visible focus and guide scope repair

Same ghartv branch/PR1, package, original signer, normal Nova5580 and web8790. Public/family rollout remains held. This is an owner-review build, not a household update.

All channels is the one complete number-sorted guide; legacy Home/All saved values resolve to it. Home's personalized duplicate tab is removed. Recent contains only successful viewing history, newest first, and remains explicitly empty when there is no history. Category selection clears the stale global query, resets to the start of that scope and keeps the chip focused. Filtering with a query respects the selected category; explicit unified search stays global. First app launch starts at All channels. Channel numbers are provider-defined and are not renumbered.

Discover Home keeps the provider document loaded for current suggestions but removes it and its descendants from keyboard/accessibility focus while covered. Native actions/cards have explicit arrow paths, on-screen scrolling and boundary behavior. Repeated Down/Up or left/right cannot enter the covered WebView; empty suggestions still leave Refresh/Open provider accessible. Returning to Discover restores the actions; a refreshed set does not replace a focused card in use. Opening a card restores the original provider page's input. Source-reading callbacks cannot lift a player tray above Home.

The guide now has one Search / mic entry. Its dialog accepts typing or the microphone with explicit Hindi/Punjabi/English/Automatic selection. Voice still uses the installed Android recognition service, with alternatives validation and typing fallback. Auto language detection/switching is requested where supported, not guaranteed. No new speech model download, server or background audio capture. Limited native-script language-name aliases help match Hindi/Punjabi channel language filters; this is not general movie-title translation.

No provider media extraction or changes to licensing, Tor, public policy deployment, accounts, data, other lanes or private analytics. Prior37 posters, Watch Now/watchlist and Punjabi Plus removal remain. Testing uses real production Activity code with a debug-only local page/image loader, never provider accounts or movie streams. New regressions exercise repeated down/up keys, refresh, returning home, real poster selection, underlying focus refusal, index ordering/history/category/search and voice-language intents. Full tests and exact build results are reported only in the generated validation file.

The compiled APK/source are bundled in the existing original-key sign-and-open entry, with38 pinned as the only review. No local Gradle build, scrcpy, new worktree/emulator or old-app reinstall. Owner signing/install/live-provider playback are not claimed by cloud compilation; the local launcher records actual outcome and attempts existing mirrors. No old candidate is deleted before replacement acceptance.
