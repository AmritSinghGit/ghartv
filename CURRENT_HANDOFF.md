# Current GharTV owner-review handoff — 15 September 2026

Read GHARTV_LANE_PROGRESS.md first. The source authority for this candidate is the EXISTING `codex/ghartv-remove-auto-preview` branch and PR1, not a new lane. Donor SHA45f689ad0310e07dec77e01fd0d310316ea8aab8. The delivered candidate is0.6.0-rc4-owner-convergence/code20. Exact source/artifact identities will be in release v0.6.0-rc4/review-manifest.json and the copied owner receipt.

Do not run the old main-pinned Cyan5 to install0.5.5RC2. Public main's live update/latest.json is independently approved RC8 recovery/code17/source de3106e3e97a9147b06347a3d66c4e5923cdbbcc, digest8cff8f85403da5924865fddc687dbff11089d7c498f9863e483a7d8123c1ce13. Nothing here promotes code20 or rewrites that feed.

Use the release-rendered GHARTV_SYNC_CURRENT_AND_REPORT.command, based on tools/owner_review.command.in, whose three artifact markers are replaced using the actual built manifest. The development checkout is only observed read-only, so branch/dirty status no longer blocks binary review. No clone/worktree/branch switch/reset/stash/unknown cleanup. Same signing key; same existing AVD; same browser port8790. The new companion runtime is a regenerable application delivery, not another repository.

The command signs locally with the existing four-field signing.env configuration only when a valid signed release asset does not already exist; secrets stay in the local child environment. It verifies public RC8 signer continuity and exact compiled payload, publishes review-only signed APK, and checks installed bytes and foreground activity. It uses existing collector configuration and Wrangler auth to apply additive schema/deploy the matching Worker only when the current revision differs. No new D1/Worker/account or secret rotation. No actual broadcast is sent by the installer. Missing backend auth/config is reported independently; no claim telemetry was live just because the page opened.

Review order: emulator guide/login; Appearance → edit dates; two channels → Picture shapes/Quality & live stats; Diagnostics/Hardware optional sharing; Jio account → Owner messages to generate a pairing code; owner page http://127.0.0.1:8790/owner.html to pair and explicitly queue a message; http://127.0.0.1:8790/ for existing browser TV. Provider login/playback may require its own valid account; no protection bypass. Public downloads/history are docs-only publication, not a new TV install.

Log receipt stores UTC timestamps for machines and IST for display. Obsidian writes/readbacks the existing lane note. Bridge is attempted once after review under bounded time; its result is separate. First Enter copies, second permits closing only the managed dedicated single-tab terminal. Cleanup only known obsolete command bytes and this run's verified managed companion predecessor after new health succeeds.

Validation must quote actual build/contract results. A compiled APK is not a real-TV playback or measured speed improvement. AI super-resolution is not implemented. Dates remain recovered baseline plus editable local overrides; no unknown date values asserted. Existing-subscription movie aggregation remains a separate pending product requirement. No commercial approval or paid offering enabled.
