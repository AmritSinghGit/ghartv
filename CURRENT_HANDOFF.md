# GharTV 0.6.0 RC6 — first focus, family management and review continuity

## Authority and baseline

Continue independent GharTV lane `ghartv`, repository `AmritSinghGit/ghartv`, existing PR1 / `codex/ghartv-remove-auto-preview`, package `in.ghartv.nova`. Preserve the same development checkout, AVD, signing key, port8790, collector/D1 and Operon Analytics tenant. No new repo, branch, worktree, service or tenant. Keep the intentional manual-report policy from commit710c94e.

The owner supplied **five screenshots and a successful local receipt**, run GHARTV-CYAN-7-20260915T131648Z-51264. RC5/code21 source7915a279e78f3490fc599c4b7f0447d7a9f18ceb, signed APK776495e2154a50e7f3460ba9b8e349678217f7c750d15061439894b015848472, was installed with byte verification and MainActivity foreground. Owner explicitly accepts category-scoped channel surfing. Do not call this baseline unsigned/unopened again. Overall owner decision remains REVIEW_PENDING, not public-release approval. No new screenshot upload is required for the feedback already provided.

RC6 is `0.6.0-rc6-family-focus` / code22. Its exact source, unsigned APK, companion and launcher digests are in the generated release manifest/DELIVERY.json for v0.6.0-rc6. Subsequent metadata/control commits are not different APK sources. New owner signing and actual Mac/TV UI review remain separate from compilation or test results.

Public TV update remains independently approved RC8/code17/source de3106e3e97a9147b06347a3d66c4e5923cdbbcc, hash8cff8f85403da5924865fddc687dbff11089d7c498f9863e483a7d8123c1ce13. No public-feed change, PR merge, physical-TV update, forced update or silent installation in this delivery.

## Screenshot feedback implemented in this source

1. **First-open Next channel focus.** Previously `showGuide(false)` explicitly requested focus on the video behind its visible panel. It now focuses Next immediately, retries once after layout when necessary, and cancels stale posted focus requests. Existing deliberate button focus is retained; metadata/timers do not repeatedly steal it. The existing auto-hide interval remains independent of focus. Picture is included in the interactive-control focus check. Selected-category order is not widened or replaced.
2. **Family management.** The builtin displayed spelling is Simrat. Internal legacy key `simrit` is intentionally retained so saved dates/photo associations do not vanish. Jio account → Appearance → Manage family provides name/date editing, Add a person, confirmed Remove, date reset, and optional restoration of removed defaults. Custom people use stable generated keys. Changes are stored on this TV only, within the existing preferences. Names accept Unicode, one line and1–60 codepoints; up to32 active people; real calendar validation. Remove a current preview exits it. No name/date enters telemetry or receipt uploads. Exact old date corrections are not invented.
3. **Browser guide/player.** Search moves to the sticky top navigation. Responsive lighter static surfaces, legible truncated card titles and a less intrusive translucent player. Arrow-key grid/control focus, Enter activation, PageUp/PageDown scoped channels, F/fullscreen button, Escape back, /search. Typing and range inputs are not hijacked. The fullscreen target is the player shell, not the HTML dialog. Focus returns to the originating card; initial Next focus is visible. Mouse-idle chrome can hide; keyboard interaction keeps controls discoverable. Selected category/search snapshot is preserved. No new video decoder or auto-preview background load.
4. **Owner console.** Overview, Televisions & messages, Playback health, Review & feedback. Clear private connection state, separate receipt identity, explicit paired-TV load, confirmation before sending, history/revocation, readable health tables. Preserve manual summary/detail reads and no D1 polling. Only local config and receipt status are loaded on entry; changing visual tabs does not query the collector. Local collector token remains server-side; nonce/origin checks protect review routes. Lock clears unsaved feedback and displayed local receipt as well as report data.
5. **No compulsory handoff copy/paste.** Launcher persists receipt.json/handoff and Obsidian before copy prompts. It now attempts an allowlisted technical receipt mirror to the SAME PR1 after local review and bridge attempt; readback checks exact body/run identity. No free text, raw logs, private paths, account/device identifiers, names/dates, screenshots or secrets are uploaded. A single managed PR comment is updated, with local pending state, idempotency and protection against overwriting a newer run. Network/CLI failure never invalidates installed-review success. Owner Review tab shows local sync status, retries explicitly, copies candidate-linked feedback and saves free text privately on the Mac/Obsidian. Local feedback is not published to the public PR automatically.

## Automatic continuity boundaries

Technical receipt publishing uses the existing authenticated GitHub CLI, with an18-second overall budget and bounded responses. No new token or backend. Missing auth or connectivity produces PENDING rather than asking for secrets or blocking the emulator. It may use the network at the END of a bundled run solely for this safe receipt; APK installation bytes remain bundled and offline-capable. Signed-APK publication and Worker deployment remain deferred in bundled mode. Actual provider playback/telemetry still need internet and valid provider authorization.

Owner page's saved feedback is attached to the actual local run/source/hash; it is written into the existing Obsidian lane when available and retained privately even if that vault is missing. It is not a direct chat message. A mirrored receipt being readable on GitHub also does not make a chat auto-read or auto-approve it. On the next user message, first retrieve the existing PR's current safe-receipt mirror; ask for a paste only if the required current run truly is unavailable. The five screenshot requests are tracked in OWNER_REVIEW_REQUIREMENTS.json and are not repeated questions to the owner.

The previous receipt's Obsidian write/readback succeeded; bridge handoff exit0 followed by timeout did not prove replica synchronization. New runner fields show `RECEIPT_SYNC` and a verified GitHub URL independently from memory bridge result. Do not equate local file persistence with remote replication or a public binary release. No continuous watcher or system-wide startup service is installed.

## Review / preservation

Use the full GHARTV_RC6_REVIEW.zip / RUN_GHARTV_REVIEW.command. It seeds exact known artifacts, reuses the original saved local signing config and typed Android verifier, updates only the existing AVD, and replaces only the previous proven managed web runtime after new health verification. Development checkout remains read-only; unknown local changes/keys/data/history remain untouched. Only known redundant delivery files are cleaned. Public17 remains unchanged.

Owner acceptance: verify Next has immediate visible focus on first open, move away and confirm no unsolicited focus reset; category next/previous still stays scoped; edit/add/remove one local family entry; search and navigate by keyboard, toggle fullscreen and return; inspect owner manual reports and private feedback saved against the right candidate; confirm safe-receipt readback or explicit pending status. Do not require re-review of a feature already accepted unless a change affects it.

## Verification / limits

Focused checks cover name/date rules, safe receipt projection/idempotency/newer-run protection with a mock CLI, source/JS syntax, real browser keyboard/scope/fullscreen/owner manual-read/private-feedback behavior with controlled data, and an actual Android release build. Consult actual workflow results before claiming they passed. Local container browser URL navigation is administrator-blocked; full HTTP/browser validation belongs to the cloud job, not a claim of local browser success.

The source fix and compile do not establish native TV initial-focus rendering, persistence UX on the owner's device, owner GitHub auth, fresh collector authorization or a new Obsidian write. Those are measured by the next owner run. No private user logs were fetched. AI super-resolution, cross-provider entitlement aggregation, commercial household billing/isolation and licensed-native-movie playback remain unimplemented and outside this focused UI continuation.
