# GharTV — current native repair, 21 September 2026

OWNER_DECISION=CHANGES_REQUIRED
INSTRUCTION_ID=GHARTV-OWNER-REPAIR-20260921
LOCAL_EXECUTION=NOT_DISPATCHED_OR_VERIFIED_BY_THIS_CLOUD_CHAT
NEXT_ACTION=EXISTING_LOCAL_OWNER_OS_AND_GHARTV_TASK_REPAIR_NORMAL_EMULATOR_AND_DNS

## Do not ask Amrit to paste another handoff or run another recovery ZIP

First fetch the live managed receipt at https://api.github.com/repos/AmritSinghGit/ghartv/issues/comments/5687119492 and verify its marker, timestamp, run ID and hashes. Then read this owner-feedback instruction, live PR1 refs, existing local amrit-memory/Obsidian checkpoint and actual runtime. A device receipt is an observation at a time, not owner acceptance. A posted instruction is not a dispatched or completed native action.

Same independent lane ghartv, repo AmritSinghGit/ghartv, existing branch codex/ghartv-remove-auto-preview, PR1, Android package in.ghartv.nova, existing AVD GharTV_Nova_Manual_google_tv_API36 on5580, existing web8790. Resolve the canonical local checkout and actual writer lease from the existing registry; do not create another checkout/task/VM/database/controller or steal a lease.

## Latest owner observations supersede the recovery recommendation

The owner reports the scrcpy window is very slow and explicitly rejects it as a substitute for the normal emulator. The supplied screenshot shows GharTV's FlixMomo activity with net::ERR_NAME_NOT_RESOLVED for https://flixmomo.app/ while the GharTV banner incorrectly says the provider is ready. The keyboard also remains over the provider area. The owner asks for normal TV review, working web and private owner analytics, cleanup, and one canonical voice/text control path across existing lanes.

The latest fetched device receipt is GHARTV-CYAN-17-WINDOW-20260921T120041Z-70882, updated2026-09-21T12:01:16Z: ACTION_REQUIRED, phaseREVIEW_OPEN, SCRCPY_EXISTING_NOVA, display_frame_received=false, window NOT_CHECKED, installed_foreground=true, web_ready=false, bridge NOT_CONFIRMED, cleanup_count=0/bytes=0. The later screenshot proves a mirrored screen eventually appeared; do not claim it never appeared merely because the earlier detector failed. Neither fact establishes acceptable performance or video playback.

Installed code30 remains source ae4c84578e26e707d1a11c5ff078f6395cf36dde, signed APK SHA256723d84553477fd5dc390c1001bfcd213919ffd434a4152905c6a243634e164ad. Signing/installation worked. Reinstalling those same bytes does not repair host window ownership or Android DNS. Earlier full candidate and window recovery records remain in REVIEW_CANDIDATE.json, TV_WINDOW_RECOVERY.md and Git history; their old REVIEW_PENDING/open-mirror recommendations are superseded by this CHANGES_REQUIRED feedback.

## Small application-source correction already published; not installed

Commit a2df1ff4b310721d33a111220539959c91b674fa on the existing implementation branch changes only FlixMomoActivity.java. Main-frame DNS/connect/timeout/TLS failures are retained through onPageFinished, instead of resetting to pageReady for WebView's error document. Search/Browse dismiss the soft keyboard. No resolver, domain, TLS, provider permission, signing identity or runtime was changed. The cloud chat has read back the exact diff; it has NOT compiled, installed or demonstrated this patch on Android. The next actual APK update must get a versionCode above the freshly observed installed/published maximum and original signer; do not silently replace code30 with different bytes.

This source correction fixes false status reporting, NOT the underlying owner-device DNS failure. Do not present it as a complete fix.

## Execute in the existing native GharTV/Owner OS task, not another downloaded wrapper

1. Bootstrap the existing amrit-memory lane and reconcile the current canonical task, lease, local repository/dirty changes, source, service inventory and the already-running deterministic Mac cleanup task. Check real capabilities first. This cloud session exposes GitHub but has no callable native terminal/ADB/Obsidian/amrit-memory tools; tool discovery confirmed that limitation. No local cleanup or runtime repair has happened through this message.

2. Read fresh owner-account ADB devices, exact AVD name, QEMU/emulator process parent/arguments,5580/5581 owners, GUI/headless/embedded flags, real window state, memory pressure and current scrcpy process identity. Stop only this rejected GharTV-owned mirror after matching UID/PID/start-time; do not kill every scrcpy/Node/Java process. Restore the standard emulator GUI. If its configuration/launch mode requires a restart, gracefully restart only this positively identified existing Nova with its persisted app/user data, original SDK and canonical ports; no -wipe-data, new AVD or hidden second VM. Never substitute another mirror as success. Do not drop security or resource checks to make the screen look ready.

3. Diagnose DNS before retrying provider playback: compare Mac and Android resolution in the same observation window for the three already-verified provider domains (flixmomo.app, flixmomo.st, www.flixmomo.st), a known control such as api.github.com, and existing Jio guide/playback hosts. Existing tools/tv_local.py has read-only host/device probes but its SERVICES map omitted FlixMomo; extend or invoke those existing probes with these explicit domains rather than writing a new audit framework. Distinguish failed name resolution from ICMP filtering, HTTPS failure, provider refusal and stream failure. Inspect emulator network/proxy/Private DNS configuration without publishing credentials. Repair only the evidenced emulator problem using the existing network capability and verified resolver configuration. An emulator-scoped -dns-server startup option may be appropriate after diagnosis; do not guess/hardcode provider IPs, disable global DNS/VPN/TLS/Safe Browsing, or use Tor/alternate hosts to conceal an unresolved failure. Provider-advertised migration is already known; recheck it rather than assuming the old .app entry resolves today.

4. Open the actual installed TV app in the normal emulator, verify responsive remote navigation and show the owner the real screen. Confirm one real authorized channel playback and FlixMomo search/result/player behavior separately. A display frame, error page, loaded catalogue or MainActivity is not successful video playback. Keep failures and provider availability explicit. Preserve the approved public homepage and in-app search/player goal.

5. Reconcile and start/reuse the existing web runtime separately; --tv-only intentionally did not start it, so web_ready=false is not proof of a broken server. Test its actual viewer route and provider flow, not SELECT1/health alone. Analytics must stay OUT of the public/viewer routes. Restore a verified authenticated GharTV tenant reporting entry through the existing Operon Analytics lane, not a client-side PIN or a replacement reporting service. Preserve manual refresh and actual freshness/error states.

6. Complete cleanup through the existing Owner OS/resource task, coordinated with Fabric. Desired steady state: the current working review/runtime and canonical development checkout per lane, not all historic candidates/environments. Remove only proven superseded, unchanged, regenerable packages/environments/runtime files after preserving their exact source and necessary evidence and verifying replacement. Stop only identified obsolete/optional owned workloads with no active tasks/dependants. Keep shared Postgres/volumes/media/credentials, active work and dirty/unpushed/ignored private files. No Docker prune, volume removal, Git reset/clean/prune, blind branch/worktree deletion or blanket age/zero-CPU rules. Report exact retired paths/PIDs, reclaimed bytes and KEPT/HELD reasons; do not report cleanup complete from a prepared script.

## Existing supporting lanes checked; no replacements

Operon Owner OS: PR39, branch codex/opr-local-001-portfolio-runner, observed headd7f3c4a214593380b528bcfe1501f2e362f2d42d. Its September21 directives/comments5759967724/5759996847 already authorize one operational control system, with GharTV and VCNow Pexip as initial real acceptance cases. This is the canonical owner control, not a new GharTV portal.

Local Fabric: PR56, branch codex/opr-local-fabric-001, head3bc5e2852e3c1125149d1db676897f49e74575e5. September21 comment5759970457 explicitly places it below PR39. Current PR still reports native R2 adoption pending. Read actual local registry before assuming it has run or replacing43918/43919 with5711.

Analytics: PR61, branch codex/opr-analytics-003-vcnow-data-control-convergence, observed headedc847b777ab780ea553fa9e18f33db2b2ebc308. Prepared guided-workspace/publication narratives are not proof that an authenticated GharTV report is currently reachable. Reconcile current head and actual installed route/tenant before exposing Open analytics. GharTV-owner handoff5752614916 already belongs there; do not displace its active implementation with a duplicate.

Cleanup/resource policy: reuse PR39's AGENTS.md and tools/lane-continuity/RESOURCE_LIFECYCLE_DIRECTIVE_2026-09-19.md. Multiple lanes may remain active. Old inventory numbers are historical, not a current Mac census. The exact current local cleanup-task execution was not retrievable from this cloud chat; the native agent must inspect it before competing mutations.

## Voice/text and evidence return

The user has already authorized implementation and local repairs in the existing lanes, whether instructions arrive by phone or Mac. This is not a request for repeated broad permissions. Both supported voice and typed requests must resolve the same lane/current task and lease, with idempotent request IDs and execution/readback evidence. Use the existing native owner-control/memory bridge, not another watcher/daemon/registry.

Prove the control path with a real read-only native status action and returned nonce/time/authority, then one bounded authorized GharTV action. A later voice/text query must read the same execution receipt, not infer completion from a GitHub comment. A canonical label, Obsidian write or source publication alone does not make every chat a Mac executor. On tool/bridge failure, preserve a queued/pending request in the existing task mechanism only, never label it dispatched or executed without an acknowledgment.

No new installer is supplied for the owner to debug. Resume the existing local Operon Owner OS/GharTV task from this file. Source-only change and queued instructions do not satisfy native acceptance. Return one actual owner-visible review location, measured resource/cleanup outcome and verified safe receipt; no routine handoff paste.

## Preserved release and safety facts

Public household feed remains existing code24/source59c130abc1283e66607315db916973553164064d; no production promotion here. Original certificate40a9d8bf6b1c557b3d6fd02acef075368dd13e28691f207a297202d0d5ec233c and data must survive. No real provider playback, physical-TV acceptance, commercial rights, payments, AI super-resolution or fully hosted browser-only integration is claimed. Historical detailed current-entry blob53358c0d21957821911411a758b2789a8d8454c9 remains in Git history. Do not follow its rejected scrcpy recommendation as the next step.
