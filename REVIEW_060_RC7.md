# GharTV GREEN review — RC7 / code23

One bundled review opens the existing Android TV emulator, browser player and owner console. BLUE is the current public household update; GREEN is the local candidate. Development is source only, not another installed app.

This candidate includes the prior unfinished network/diagnostics repair: bounded system-DNS versus emulator checks, an in-app connection check, verified update-service fallback, correct telemetry acknowledgement handling, manual summary plus a bounded sample, diagnostic reference lookup, retained Punjabi/category/search navigation and family/first-Next focus. It adds a release desk in the existing owner console: verify the exact TV/web identities, open the TV again, review the three surfaces, then explicitly approve and publish the same signed APK without rebuilding.

Production publication requires the local three-surface approval and confirmation bound to run/source/signed checksum. It uploads and reads back that same signed APK, updates only update/latest.json using the current file SHA, and verifies readback before saying public. Another version, changed feed or conflicting release asset is preserved. No keys, logs, personal feedback or collector data are published.

This is a prerelease, NOT owner approval. The unsigned cloud APK must be signed by the bundled launcher using the existing saved key, not installed manually. Local installed bytes, existing certificate and foreground must be verified. Public RC8/code17 is not changed by running review. No cousin-server hosting, public tunnel, physical-TV action, automatic merge or commercial clearance. The browser and owner console remain on localhost8790; publishing the Android update is not deploying a streaming website.

A future publication uses the exact code23 review binary; the displayed version name remains the same. Existing household apps offer that update on their next successful app check, not an instant push to offline TVs. Android installation confirmation remains required. Removing a public download would not roll back already-updated TVs; a corrective Android release needs a higher version code.

Review uses the same Mac runtime and named AVD, not two copies. The typed Android certificate verifier, saved signing configuration, app data, existing Obsidian and safe technical receipt remain. No reset, clean, stash, branch switch or source upload from the Mac. Manual refresh stays manual; the one requested support capture is separate from background polling.

Cloud compile, synthetic browser/report checks and mocked promotion rejection checks are engineering evidence. They are not verification of your Mac DNS, private collector records, provider login, actual playback or an executed public release. Those states are reported separately by the local run.
