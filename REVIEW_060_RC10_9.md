# GharTV Review36 — original posters and the same player surface

Same ghartv lane, repository, PR1, branch, package and existing normal Nova. Source36 continues the actual installed code34 plus the earlier uncompiled local code35 poster-navigation work. It is a precompiled review; the Mac does not build it. Original private signing remains local and mandatory.

## What changes onscreen

The text-only native result screen is removed, not temporarily hidden. FlixMomo's existing poster images, headings, descriptions and metadata remain in its original document. Arrow/OK navigation adds focus to the original posters; it does not rebuild or replace them. Search and Use page collapse the app header; Menu restores search on the results page or opens the player tray on a title page. The header and tray show Review36 so old code cannot be mistaken for this repair.

The existing native GharTV tray keeps Play, Players, Not playing, Search, Use page and Hide. It controls only players actually exposed by the current provider page; Player1/2/6 are not invented. Same-page labelled links and role=tab controls are also recognized. The underlying provider video engine remains. This release does not claim Media3 can play arbitrary protected/undocumented sources. Cross-origin video status remains unobservable; Not playing is the explicit user request for the next offered choice. No blind cycling on timeout or challenge.

The provider-advertised flixmomo.bet/www.flixmomo.bet destinations seen in the owner's screenshots are added to the same explicit HTTPS host list; no wildcard or arbitrary redirect is allowed. Background blocked links do not replace the main playback message with a persistent generic error. DNS, TLS and HTTP failures still stop and display their real error. No security/DRM/CAPTCHA bypass or media-URL extraction.

## Review command and verification

The release entry GHARTV_OPEN_REVIEW.command defaults only to36 and accepts --candidate36 written as two arguments. It pins this release's APK and source archive, signs with the existing key, verifies certificate/package/code/payload, updates the same emulator in place and uses the existing web/tab handling. Wrong/older candidate arguments fail rather than silently opening33/34. No local Gradle build, new emulator, new key, scrcpy, household rollout or old-candidate deletion.

Cloud tests use actual Android36 WebView and the production coordinator against local fixtures. They verify original image/metadata elements survive repeated observations, actual remote activation, source selection boundaries, and retained cursor behavior. They are not live FlixMomo movie playback or owner-Mac execution. Results are only claimed in VALIDATION.json after passing.

Code33 remains separately family-approved; code36 requires review and is not promoted to Dad's update feed. All original private state/data/signing and the public homepage are preserved. The latest fetched native receipt was code34; lack of a newer mirror is not evidence of why the local code35 attempt stopped. No native cleanup/Obsidian completion is claimed from cloud source publication.
